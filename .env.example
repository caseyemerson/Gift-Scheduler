# Server port (Railway sets this automatically)
PORT=3001

# Path to SQLite database file
# In Railway, mount a persistent volume and set this to a path within it (e.g. /data/gift_scheduler.db)
DB_PATH=/data/gift_scheduler.db

# Node environment
NODE_ENV=production

# --- Retailer Integrations (all optional) ---

# Amazon Product Advertising API
# Sign up: https://affiliate-program.amazon.com/
AMAZON_API_KEY=
AMAZON_API_SECRET=
AMAZON_PARTNER_TAG=

# Etsy Open API v3
# Sign up: https://www.etsy.com/developers
ETSY_API_KEY=

# Walmart Affiliate API
# Sign up: https://affiliates.walmart.com/
WALMART_API_KEY=

# --- Shopping Aggregator (optional) ---

# Google Shopping (Programmable Search or SerpAPI)
GOOGLE_SHOPPING_API_KEY=
GOOGLE_SHOPPING_ENGINE_ID=

# --- LLM Provider (configure one) ---
# The app checks in order: Claude > ChatGPT > Gemini > OpenAI-compatible > template fallback
# Set LLM_PROVIDER to force a specific one: claude, openai, gemini, openai_compatible

LLM_PROVIDER=

# Anthropic (Claude)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929

# OpenAI (ChatGPT)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Google (Gemini)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

# OpenAI-compatible (Together AI, Groq, Ollama, vLLM, etc.)
OPENAI_COMPATIBLE_BASE_URL=
OPENAI_COMPATIBLE_API_KEY=
OPENAI_COMPATIBLE_MODEL=
