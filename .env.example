# Server port (Railway sets this automatically)
PORT=3001

# Path to SQLite database file
# In Railway, mount a persistent volume and set this to a path within it (e.g. /data/gift_scheduler.db)
DB_PATH=/data/gift_scheduler.db

# Node environment
NODE_ENV=production
